repository에 올리지 않을 폴더/파일들을 .gitignore 파일에 작성합니다.

gitignore.io 사이트에서 .gitignore 파일을 쉽게 만들 수 있습니다.

해당 사이트에서 사용 언어(ex. Java), 운영체제(ex. Windows), 프레임워크(ex. Intellij) 등을 설정한 뒤 생성 버튼을 누르면
자동으로 .gitignore 파일이 생성됩니다.